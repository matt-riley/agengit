# Plan 004: Remove stale "planned" roadmap entry for already-shipped blob content search

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md`.
>
> **Drift check (run first)**: `git diff --stat 3b45f66..HEAD -- README.md src/cli/grep.zig`
> If either file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.

## Status

- **Priority**: P3
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: docs
- **Planned at**: commit `3b45f66`, 2026-06-21

## Why this matters

`README.md`'s Roadmap section lists "content search across captured workspace blob bodies" as planned-but-not-shipped, with a note that `agit grep` today only searches "activity" (messages/tool calls), not blob content. This is no longer accurate: `agit grep --content` already searches captured workspace blob bodies — it's implemented in `src/cli/grep.zig` (the `--content` flag, `options.content`, wired to a "Search recorded ... captured file content" code path) and covered by `tests/e2e/grep/content.zig`. A stale roadmap entry for an already-shipped feature erodes trust in the README for both human readers and agents that read `README.md`/`AGENTS.md` to discover capabilities (per `CLAUDE.md`'s framing of this project's audience).

## Current state

- `README.md:294-305` already documents `agit grep` in the command reference (inside the generated `<!-- BEGIN COMMANDS --> ... <!-- END COMMANDS -->` block) as: "Search recorded messages, tool activity, and captured file content across all sessions." — this description is already correct and up to date; it is generated by `zig build docgen` from `src/cli/specs.zig`, so do not hand-edit it.
- `README.md:396-407` (the Roadmap section, hand-written, not generated):
  ```markdown
  ## Roadmap

  Planned but not shipped today:

  - content search across captured workspace blob bodies (activity search via
    `agit grep` ships today; searching the content-addressed snapshot blobs is
    the open item)

  Experimental today: `agit observe --once jsonl --input observer.jsonl` exercises
  the observer framework and persists replay checkpoints under
  `.agit/observers/`.

  Structured CLI output uses the `cli-json-v1` envelope documented in
  [`docs/format/cli-json-v1.md`](docs/format/cli-json-v1.md).
  ```
- `src/cli/grep.zig` implements `--content` (search `grep -n "content" src/cli/grep.zig` to see: `options.content: bool = false` field, the `if (options.content) { ... }` branch around line 87 that runs a blob-content search path distinct from the message/tool-call search, and the `--content` flag parser around line 363).
- `tests/e2e/grep/content.zig` exists and exercises this `--content` flag end to end — confirming it's shipped, not experimental.
- `CHANGELOG.md` has an entry (`a5e4377 feat: add blob-body content search to grep (#86)`) confirming when this shipped.

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Docgen freshness check | `zig build check-docgen` | exit 0 (confirms you haven't touched the generated section) |
| Full check | `zig build check` | exit 0 |

## Scope

**In scope** (the only file you should modify):
- `README.md` — the hand-written "Roadmap" section only (lines ~396–407, outside the generated `<!-- BEGIN COMMANDS -->`/`<!-- END COMMANDS -->` block).

**Out of scope** (do NOT touch, even though related):
- The generated command reference block (between `<!-- BEGIN COMMANDS -->` and `<!-- END COMMANDS -->`) — it is already correct and is regenerated by `zig build docgen`, not hand-edited.
- `src/cli/grep.zig` or any other source file — no code changes are needed, this is a docs-only fix.
- The "Experimental today" and "Structured CLI output" paragraphs that follow the bullet list — leave them as-is unless removing the bullet list leaves the Roadmap section with only those two paragraphs, which is fine (see Step 1).

## Git workflow

- Branch: `advisor/004-fix-stale-readme-roadmap`
- Commit message: `docs: remove stale roadmap entry for shipped blob content search`
- Do NOT push or open a PR unless explicitly instructed.

## Steps

### Step 1: Remove the stale bullet and its now-empty "Planned but not shipped today:" lead-in

Edit `README.md`'s Roadmap section. Remove the line `Planned but not shipped today:` and the bullet beneath it (the three lines about content search), since after removing the one stale item there are no other "planned but not shipped" items in this section. The result should read:

```markdown
## Roadmap

Experimental today: `agit observe --once jsonl --input observer.jsonl` exercises
the observer framework and persists replay checkpoints under
`.agit/observers/`.

Structured CLI output uses the `cli-json-v1` envelope documented in
[`docs/format/cli-json-v1.md`](docs/format/cli-json-v1.md).
```

If, when you open the file, there are *other* "planned but not shipped" items already present beyond the one described in "Current state" (i.e., the codebase or README has drifted), keep the lead-in line and only remove the specific stale bullet about content search — do not remove the whole section in that case.

**Verify**: `grep -n "the open item\|content-addressed snapshot blobs is" README.md` → no matches (confirms the stale bullet text is gone).

### Step 2: Confirm the generated command section is untouched

**Verify**: `zig build check-docgen` → exit 0 (this fails if the generated README section drifted from `src/cli/specs.zig`; since you didn't touch that section, it should still pass).

## Test plan

- No code tests apply — this is a documentation-only change. The verification is the `grep` check in Step 1 plus the existing `check-docgen` build step confirming no generated content was disturbed.

## Done criteria

- [ ] `grep -n "the open item\|content-addressed snapshot blobs is" README.md` returns no matches
- [ ] `zig build check-docgen` exits 0
- [ ] `zig build check` exits 0
- [ ] `git status` shows only `README.md` modified
- [ ] `plans/README.md` status row for 004 updated to DONE

## STOP conditions

Stop and report back (do not improvise) if:
- The Roadmap section in the live `README.md` no longer matches the "Current state" excerpt (e.g., it has been reworded or other items added) — re-read it and decide whether the fix still applies cleanly; if the structure has changed substantially, stop and report instead of guessing at intent.
- `agit grep --content` turns out to not actually work end-to-end (e.g., `tests/e2e/grep/content.zig` is failing or skipped) — that would mean the feature isn't actually shipped and the roadmap entry might be correct after all; verify with `zig build test-e2e` filtered to that test before editing if you have any doubt.

## Maintenance notes

- This is a one-off doc fix; no follow-up work is implied.
- If new "planned but not shipped" items are added to this section in the future, prefer linking them to a tracking issue or ADR rather than a loose prose bullet, so staleness is easier to catch (e.g., cross-check against `CHANGELOG.md` periodically).

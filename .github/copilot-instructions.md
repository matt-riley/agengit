# Copilot Instructions for `agengit`

Use `CLAUDE.md` as the canonical project guide for build/test commands, architecture, testing layout, conventions, and docs expectations.

Copilot-specific note: this repo records GitHub Copilot through a generated extension (`src/cli/copilot_extension.zig`) and the `copilot-hook` adapter; keep behavior changes aligned with fixtures in `src/fixtures/hooks/` and e2e hook coverage.

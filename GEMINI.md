# GEMINI.md

See `CLAUDE.md` for the canonical project guide: build/test commands, architecture, conventions, ADR policy, and release notes.

Gemini-specific note: this repo records Gemini through `agit init` and the `gemini-hook` adapter; keep any Gemini behavior changes aligned with `src/hook/adapters/gemini.zig`, fixtures in `src/fixtures/hooks/`, and e2e hook coverage.
